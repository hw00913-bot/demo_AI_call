## 2026-05-20 工作记录

### 功能说明文档
- 修复功能说明.csv乱码：添加UTF-8 BOM（EF BB BF）
- 生成功能说明.md：基于CSV转换为Markdown格式

### 弹窗修复
- 修复意向数据设置弹窗不居中/被遮挡问题
- 根因：`style.css` 第2130-2131行，`.import-record-summary` 声明块后多了一个 `}`，导致 `.intent-config-backdrop` 及后续所有CSS规则解析失败
- 修复：删除多余的 `}` 符号

### 意向分类配置弹窗完善
- 将意向等级从标签展示改为单选下拉框
- 可选标签：H(高意向)、A(高意向)、B(低意向)、C(中意向)、D(无意向)、E(无效)、F(拒接)、L(未接通)
- 默认值：意向等级1=A(高意向)，意向等级2=B(低意向)，意向等级3=A(高意向)
- 保存后意向分类统计标题自动同步更新
- 功能说明文档同步更新

