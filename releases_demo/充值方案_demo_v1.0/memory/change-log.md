# 项目变更记录

## 2026-06-05

- 完成租户列表计费汇总、充值单配置、有效期、通话余额、冻结金额、可用余额和呼叫控制原型。
- 完成已支付充值单唯一关联、手动生效和历史记录展示逻辑。
- 完成标注弹窗点击打开、关闭、拖动、缩放、结构化编辑、浏览器缓存和一键显隐。
- 重组项目目录，将样式、Mock、文档、记忆、标注和维护工具分离。
- 保持 `index.html` 为唯一演示入口，业务逻辑未因目录调整而改变。
- 将旧 `.workbuddy` 中仍有效的角色权限背景迁移至统一项目记忆，并移除旧记忆目录。

## 2026-06-11

- 租户改为独立配置通话单价。
- 页面余额统一按分钟数展示，充值金额根据租户通话单价换算为通话分钟数。
- 通话计费拆分为大模型和小模型两套独立单价、余额及可用分钟数。
- 调整为统一资金账户：充值不区分模型，冻结按模型换算金额，可用金额再分别换算大、小模型分钟数。
- 单价扩展为租户、模型、供应商三级配置；冻结任务锁定供应商与单价快照。
- 租户列表展示大、小模型的供应商可用分钟范围，充值配置抽屉展示供应商计费明细矩阵。

## 2026-06-12

- 将供应商计费改造的最终实现写入项目记忆。
- 明确供应商价格配置是唯一价格来源，租户主数据不再保留单一模型单价。
- 固化统一资金余额、冻结总额、统一可用金额和供应商分钟数的计算公式。
- 记录供应商调价不追溯已冻结任务、不同供应商分钟视图不可相加等约束。
- 租户列表“供应商计费配置”改为“计费配置”按钮，并新增独立配置弹窗。
- 计费配置弹窗支持编辑模型供应商单价及启停状态，保存后实时刷新分钟范围。
- 充值单配置抽屉拆分为“计费明细”和“充值单管理”两个 Tab，充值关联与历史记录统一归入充值单管理。
- 页面入口及抽屉名称由“充值单配置”统一调整为“充值管理”。
- 生效操作下沉到每条历史关联充值单，移除租户列表和抽屉顶部的租户级生效入口。
- 充值单关联后不再立即更新账户；逐单生效后按计费类型更新有效期、余额或两者。
- 大模型和小模型供应商模拟数据统一调整为科大讯飞、一知科技、中科金、冰兰四家。
- 当前版本计价收敛为租户的大模型、小模型默认价；供应商目录改为未来扩展字段，不再生成重复供应商价格。
- 预留供应商覆盖规则结构，未来按“供应商专属价优先、模型默认价兜底”扩展。
- 新增余额调整流水，支持线下退款扣减、其他手工扣减和手工补回。
- 余额公式纳入调增、调减流水；线下退款不修改原充值单，只登记已生效调减记录。
- 手工扣减只校验资金余额，不考虑冻结占用；扣减后余额不足覆盖冻结时展示风险、可用分钟归零并禁止新增呼叫。
- 将线下退款入口移动到已生效通话充值单的操作列，退款弹窗自动绑定当前充值单。
- 通用余额调整移除充值单选择和线下退款类型，仅保留其他手工扣减、手工补回。
- 调整充值管理演示标注到包含通话充值的租户，确保可直接体验充值单行内退款。
- 仅坐席费充值单明确显示”不含通话费”，可手工扣减金额不再展示负数。
- 余额调整拆分为独立 Tab，与计费明细、充值单管理并列。
- 余额调整简化为仅支持手工扣减，通过弹窗输入金额和原因；移除调整类型选择、手工补回和线下退款扣减。
- 手工扣减不再关联充值单，只校验资金余额。
- 历史关联充值单移除退款扣减入口，操作列仅保留生效按钮。
- 冻结覆盖不足判断增加 totalFrozenAmount > 0 条件，避免余额为负但无冻结时误报。
- 租户信息区新增历史充值总额和可用余额两个只读字段。
- 余额调整记录列表移除关联充值单列。
- 清理 `showRefundModal`、`submitOfflineRefund`、`getRechargeRefundedAmount` 等退款相关函数。
- 彻底移除手工补回、调增方向和旧余额调整表单的不可达代码。
- 手工扣减流水统一使用 `MANUAL_DEDUCT/OUT`，不关联充值单。
- 可用余额改为隐藏字段，仅供内部计算与刷新，页面不再展示金额。
- 移除“冻结覆盖不足”作为独立全局禁呼状态，改为每次导入按预计冻结金额校验。
- 手动导入弹窗新增租户、模型、供应商、单价来源、号码数、预计冻结金额和最大可导入数展示。
- 导入成功后按任务计费单价快照新增冻结记录；超出可用金额时拒绝并提示最大可导入数。
- 提升 Mock、场景脚本、租户计费脚本和样式资源版本号，避免浏览器继续命中旧缓存。
