# 变更记录

## 2026-06-15 — 标签管理模块 v1 初始化

- 新增系统管理 → 标签管理入口（`sys-tags` 路由）
- 新增 `js/pages/sys-tags.js` 页面模块，包含：
  - 左侧配置树（供应商 → 租户类型 → 场景 三级树结构）
  - 右侧标签列表（启用勾选 + CRUD 编辑删除新增）
  - 场景配置弹窗（CRUD + 启用/停用）
  - 供应商管理弹窗（CRUD + 启用/停用）
  - 复制配置到其他节点
- 新增 Mock 数据：5个供应商（科大讯飞、冰兰-vcp、冰兰、一知科技、中科金）、5个场景（督办、服务、回访、新线索、冷线索）、2个租户类型
- 清理了已删除的 annotations 标注系统残留引用
- 数据模型预留 `platformTagId` 字段（未来平台标签层扩展）
