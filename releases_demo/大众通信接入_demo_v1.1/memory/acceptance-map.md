# 需求与验收映射表 (Acceptance Map)

本文件定义“大众通信接入”原型 v1.1 的 WIKI 对齐验收关系。

## 需求与验收标准映射表

| 需求 ID | 需求描述 | 验收标准 | 验证方法 | 验收状态 | 备注原因 |
|---|---|---|---|---|---|
| R-001 | 13个通话状态码中文解析与中台状态归并 | 0等待呼叫...12用户屏蔽正确显示，未知码显示原值；中台新增“等待呼叫”状态承接大众 0-等待呼叫，并新增“秒挂”“伪接通”承接电声接通细分状态；跨平台统计、筛选或对账时按完整中台状态归并：已接通、秒挂、伪接通、无人接听、占线、拒接、空号、关机、停机、欠费、无法接通、黑名单过滤、拦截规则、等待呼叫、待呼叫去重、分机号错误、呼叫受限、主叫欠费、呼损客户、外呼失败、转人工呼损、线路拦截、等待重呼、号码故障、线路故障；中科金接入原型中的 4/5/6/7/8/9/11/12/13/14/15/17/18 状态码已同步到说明文档映射表；电声接入原型中的 answerStatus 205/206/301/302/303 与 answerMainStatus 2/3 兜底规则已同步到说明文档映射表 | 检查通话记录列表状态列文本，数字被解析为中文；检查说明文档状态映射表以本地状态为主列，补齐一知/科大/中科金/电声/大众通信状态对应关系；确认待呼叫去重不再承接大众 0；确认中科金 1/2/3 过程态不进入最终通话结果表；确认电声 BLOCKED 不单独判定具体拦截状态 | pass | 大众通信特有的13种通话状态码展示成功；文档已补全到新增“秒挂”“伪接通”“等待呼叫”后的 25 项中台状态集合，并补充中科金、电声回传状态列 |
| R-002 | A-F等级意向数据展示 | 线索记录、大众任务意向分类配置和意向洞察将 1–6 展示为 A-高意向、B-意向客户、C-潜在客户、D-一般意向、E-需再次跟进、F-号码无效；空值显示“未评级” | 检查大众任务数据概览的分类卡片、配置下拉和 6 个洞察标签，并回归一知、中科金原有标签；检查线索记录 1–6 转换 | pass | 大众分类卡片与三个配置行使用完整枚举，每行下拉均为 6 项；中科金原有分类和洞察标签不变 |
| R-003 | 通话记录平台筛选器 | 通话记录顶部新增所属平台下拉筛选，可过滤大众通信通话记录 | 选择大众通信筛选，检查记录是否已正确过滤 | pass | 平台筛选下拉框新增大众通信，数据过滤响应迅速 |
| R-004 | 大众通信任务关联 | 大众通信面板仅包含必填 uuid；uuid 缺失时阻止提交；重呼、并发、呼叫时间等配置不在中台表单出现 | 打开场景抽屉切换大众通信，核对唯一输入项、SaaS 提示及提交校验 | pass | 使用 FLD-004 |
| R-005 | 大众标签手工配置与映射 | 标签管理树展开大众通信；标签不从接口读取；支持手工新增、编辑、删除、排序、启停及本地标签映射，且不触发自动映射 | 选中大众通信，检查标签 CRUD、启停、排序和映射控件 | pass | 新增入口、6 组 CRUD/排序/启停及 6 个映射下拉均可用；无标签接口请求 |
| R-006 | 外呼任务只读对齐 | 远端 1/2/3 正确展示；大众卡片具有查看、禁用编辑和更多入口，更多菜单的删除、暂停、终止、启动均置灰且没有点击行为；名单操作不展示 | 查看 3 张大众卡片，打开更多菜单并检查禁用状态、事件与名单入口 | pass | 3 张卡片均有查看/禁用编辑/更多；菜单 4 项均为禁用灰色且 onclick=0；其他平台菜单 onclick=4 |
| R-007 | 通话按任务 ID 查询，统计精简展示 | 通话记录展示关联任务 ID 并可按 uuid 查询；通话统计按截图不展示智能平台和关联任务 ID 字段，仅保留呼叫时间、场景名称筛选；计费统计按截图仅展示计费日期、租户名称、计费时长和详情操作 | 在 result-records 查询大众任务；打开 report-call 核对精简筛选和字段；打开 report-billing 核对备注、筛选项和主表字段 | pass | uuid 查询命中通话记录；通话统计页面去掉平台/任务 ID 字段但数据层仍可按 uuid 汇总；计费统计去掉平台/计费类型展示，说明已对齐 |
| R-008 | 任务详情接口与交互对齐 | 详情数据统一取自新版 2.0 外呼任务编辑接口 `new_task_extra`，并使用 3.0 大模型接口口径；不再兼容旧版 2.0 `task_extras`；与一知、中科金使用相同抽屉、Tab 和 7 项单列信息行；不展示接口未定义的“启动方式”；自动重拨完整展示 6 类接口值，即使关闭也不隐藏；无额外详情数据和修改控件 | 打开 3 张大众任务卡片，核对 7 项业务信息、无“启动方式”，以及重呼启用、优先策略、间隔、次数、挂断原因、对话状态；检查无完整字段展开区、无旧版 task_extras 兜底和只读边界；回归一知/中科金详情 | pass | 3 张大众任务均为新版 2.0 外呼接口 new_task_extra 数据、7 项接口字段且无“启动方式”；3.0 大模型口径已写入说明；无额外详情区和编辑控件 |
| R-009 | 回调驱动通话详情 | 大众通信通话记录必须已包含回调 `callid`，查询 `recordid` 与其一致；详情页直接展示录音、通话文本、外呼结果和详细信息，不展示顶部回调/详情获取状态；无回调时不生成通话记录 | 检查列表中的大众记录均含 `callid`，打开详情核对 `callid=recordid`、录音取回调、文本取详情 records、详细信息字段顺序和无顶部状态提示；确认无等待回调页面状态；回归其他平台 | pass | 无 callid 数据已在列表准入层过滤；页面无等待回调分支；ID 映射、详情展示与非大众回归均通过 |
| R-010 | 录音播放、通话文本与外呼结果查看 | 录音取回调链接，可播放/暂停/跳转进度；文本取详情 records；外呼小结优先取大众详情 componet/component 自定义组件总结，未返回时取百炼智能体总结；线索记录“最后回访记录”同样对应通话总结；意向标签固定取百炼标签；详细信息中的通话标签取 component 已返回值的非总结自定义字段名称并以胶囊展示；线索记录的大众客户详细标签、外呼结果和详细信息与通话记录页保持同一口径 | 打开大众成功通话并操作播放器，核对 3 条文本、去重、标签清理、大众总结优先、百炼总结兜底、百炼标签和 component 通话标签胶囊；打开线索记录大众行，核对最后回访记录为通话总结；打开线索记录大众客户详细标签及线索内通话详情，核对 component 字段名胶囊、外呼结果和详细信息字段顺序；打开未接通记录与一知/中科金记录回归 | pass | 成功通话优先展示 call_summary 自定义组件总结；无大众组件总结记录展示百炼总结；线索最后回访记录取同一通话总结；意向标签取 bailianTagName；通话标签和线索客户详细标签展示 component 自定义字段名称；录音文本和中科金回归通过 |
