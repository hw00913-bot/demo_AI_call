# 待确认问题与开放项 (Open Items)

## 待解决问题
- 无。（所有讨论期提出的问题均已解决并归档）

## 已解决项归档 (Archived)
1. **迭代开发模式与项目底座确认**：已确认基于底座 `意向标签管理_demo_v1.0` 迭代开发。
2. **多平台展示与裁剪范围**：已确认保留已有其他平台（如一知科技、中科金智能等），并列新增并演示“大众通信”平台的接入与切换。
3. **新增大众通信业务场景交互**：已确认参考中科金配置表单结构，只提供一个基础的“大众通信任务 ID/uuid”输入框，去除多余的复杂下拉配置。
4. **数据展示与过滤**：已确认通话记录页面新增平台筛选器，当筛选“大众通信”时，正确解析并展示 13 个通话状态码的中文名称；线索记录不新增筛选器，并列展示大众通信对应的 A-F 线索意向等级。
5. **意向标签自动映射**：已确认大众通信首次启用时无需自动映射，完全由管理员在意向标签管理模块进行手动关联映射。
6. **迭代名称配置**：已确认迭代名称配置为 `v1.0`。
# 开放项（v1.1）

- 待联调：真实大众通信账号下的线路 id、剩余并发、价格与有效期；原型中的 DT-NJ-001 / DT-SZ-001 / DT-SH-001 仅为演示。
- 待联调：通话详情中 `duration` / `bill` 毫秒值与实际计费账单的对账精度。
- 已知边界：WIKI 页状态为 `partial`，但本轮使用的字段均取自官方 v2 或官方截图核对部分。
