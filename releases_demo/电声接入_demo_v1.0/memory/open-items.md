# 待确认事项

> 记录当前项目开发或迭代过程中尚未明确、待产品经理或业务方确认的事项。未确认的假设在澄清前不得实现。

## 待确认列表

- 当前无阻塞的接口待确认项。规则拦截已移除；按钮文案保留“确定并生成任务”；自动重呼按 `nDayMCallPolicy` 单条配置提交。

## 暂存的默认假设

- **默认假设 1**：电声平台不在新增业务场景表单中提供“线路类型”配置，也不在通话记录列表中展示线路类型。
- **默认假设 2**：加微状态 `wechatStatus` 由话单回调只读展示；“是否重拨”根据执行明细的 `attemptCount` 派生。
- **默认假设 3**：电声任务 id 不由前端输入；前端维护 `strategyCode`，同一导入批次可分次导入，最后一次 `isCompleted=true` 后进入执行流程。
